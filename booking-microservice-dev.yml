booking:
  property: Booking profile dev updated 2

spring:
  application:
    name: booking-microservice
  datasource:
   url: jdbc:mysql://localhost:3306/bookingdb
   driverClassName: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

app:
  testProp: Test property